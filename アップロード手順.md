# Xserverアップロード手順

このフォルダは、Xserverの `public_html` にそのまま置ける静的サイト + PHPフォーム構成です。

## アップロードするもの

`sumixホームページ制作` フォルダの中身を、Xserverの対象ドメインの公開フォルダへアップロードします。

```text
index.html
privacy.html
contact.php
favicon.svg
robots.txt
sitemap.xml
assets/
yurulabo/
home/
```

Xserverでは通常、対象ドメインごとに以下のような場所が公開フォルダになります。

```text
sumix.jp/public_html/
```

重要なのは、`sumixホームページ制作` フォルダ自体を入れるのではなく、その中身を `public_html` 直下へ入れることです。`yurulabo/` と `home/` も、旧Google SitesのURLをなるべく保つために一緒にアップロードします。

## DNS切り替え前の確認

1. Xserver側で `sumix.jp` をドメイン追加する
2. `sumix.jp/public_html/` にこのフォルダの中身をアップロードする
3. Xserverの「動作確認URL」または一時ドメインで表示を確認する
4. 問題なければSquarespace DNSでWeb用レコードだけ切り替える

Squarespaceで変更するのはWeb表示用だけです。

```text
A     @      XserverのIPアドレス
A     www    XserverのIPアドレス
```

Google Workspaceのメール用レコードは消さないでください。

```text
MX
TXT SPF
TXT DKIM
CNAME Google確認用
```

## 問い合わせフォーム

`contact.php` は `info@sumix.jp` 宛にメールを送る設定です。Xserver上でPHPが有効な状態で動作します。

ローカルでHTMLを開いただけの場合、フォーム画面は見られますがメール送信はできません。
